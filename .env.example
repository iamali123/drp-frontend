# API base URL (no trailing slash). Used for all API requests.
# Example (Orior dev):
# VITE_API_BASE=https://app-orior-dev-eastus2.azurewebsites.net
VITE_API_BASE=

# Organization context header sent with API requests (optional).
# VITE_ORGANIZATION_ID=your-org-id
VITE_ORGANIZATION_ID=
